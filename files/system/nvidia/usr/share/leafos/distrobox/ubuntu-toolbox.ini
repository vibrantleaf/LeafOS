[ubuntu-toolbox]
image=ghcr.io/ublue-os/ubuntu-toolbox:latest
init_hooks="curl -fsSL https://apt.fury.io/nushell/gpg.key | gpg --dearmor -o /etc/apt/trusted.gpg.d/fury-nushell.gpg && echo "deb https://apt.fury.io/nushell/ /" | tee /etc/apt/sources.list.d/fury.list && apt update
&& apt remove -y --purge --auto-remove nano vim vim-common tmux && add-apt-repository ppa:maveonair/helix-editor && apt update && apt install -y helix nushell"init=false
nvidia=true
pull=true
root=false
replace=true
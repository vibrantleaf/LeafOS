modules:
  - type: dnf
    remove:
      packages:
         - vim-miminal
         - vim-data
         - nano
    install:
      packages:
        - helix
        - screen
  - type: systemd
    system:
      disabled:
        - podman.socket
    user:
      disabled:
        - podman.socket

modules:
  - type: script
    snippet:
      - "mkdir -p /usr/lib/modules-load.d/"
      - "mkdir -p /usr/lib/modprobe.d/"
      - "echo 'ntsync' | tee /usr/lib/modules-load.d/ntsync.conf"
      - "echo 'ntsync' | tee /usr/lib/modprobe.d/ntsync.conf"
  - type: initramfs
